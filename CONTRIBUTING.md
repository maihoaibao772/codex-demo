QĐ: Không thêm tệp nhị phân (ảnh/font/audio). Mọi icon dùng emoji/CSS/SVG inline.
- PR chỉ chạm file `.html/.css/.js/.json/.md/.xml/.txt`.
- Giữ JSON từ vựng/câu đúng chuẩn (không thêm ngoài bộ người dùng yêu cầu).
